[project]
name = "35lb-chicken"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ffmpeg>=1.4",
    "ffprobe>=0.5",
    "json5>=0.14.0",
]

[dependency-groups]
dev = [
    "coverage>=7.14.0",
]
